'use strict';

import {ArmoryFramework} from './ArmoryFramework'

(function () {
  alert('start')
  const config = ''
  const armFrm = new ArmoryFramework(config)
  armFrm.AssembleFramework()
  alert('done')
  return 0
//  this.SetConfig(config);
//  armFrm.AssembleModules();
})()
